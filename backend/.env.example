# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
DB_USER=postgres
DB_PASSWORD=yourpassword
DB_HOST=localhost
DB_PORT=5432
DB_NAME=accesscore_db

# JWT Secret (for future use)
JWT_SECRET=your_super_secret_jwt_key
JWT_EXPIRE=30d
